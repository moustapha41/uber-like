# Backend BikeRide Pro - Exemple de variables d'environnement
# Copier : cp .env.example .env (dev) ou cp .env.production .env (prod)

NODE_ENV=development
PORT=3000
HOST=0.0.0.0

DB_HOST=localhost
DB_PORT=5432
DB_NAME=bikeride_pro
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe

JWT_SECRET=un-secret-jwt-long-et-aleatoire
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=un-autre-secret-pour-refresh
JWT_REFRESH_EXPIRES_IN=30d

REDIS_HOST=localhost
REDIS_PORT=6379

CORS_ORIGIN=
APP_BASE_URL=http://localhost:3000

LOG_LEVEL=info
