# Production - BikeRide Pro Backend
# Copier vers .env : cp .env.production .env
# Puis remplir les valeurs réelles (mots de passe, secrets, URLs).

NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# Base de données PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bikeride_pro
DB_USER=postgres
DB_PASSWORD=

# JWT (utiliser des secrets forts en prod)
JWT_SECRET=
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=30d

# Redis (optionnel selon les features utilisées)
REDIS_HOST=localhost
REDIS_PORT=6379

# CORS : URL(s) du front admin / app (séparées par des virgules si plusieurs)
# Ex: https://admin.votredomaine.com ou http://IP:5173
CORS_ORIGIN=

# URL publique de l'API (pour callbacks paiement, liens dans les mails, etc.)
APP_BASE_URL=https://api.votredomaine.com

# Paiement PayTech (optionnel)
# PAYTECH_API_KEY=
# PAYTECH_API_SECRET=
# PAYTECH_ENV=live

# Cartes (optionnel)
# MAPBOX_ACCESS_TOKEN=
# GOOGLE_MAPS_API_KEY=

# Logs
LOG_LEVEL=info
